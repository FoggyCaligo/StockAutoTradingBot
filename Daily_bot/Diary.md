이 파일은 변경 이력과 실험 메모를 남기는 용도다. 현재 전략의 최신 기준값은 `config/settings.yaml`, `README.md`, `docs/strategy_design.md`를 우선한다.

2026-06-15 18:05:00 +09:00
- `top-5 ask depth` 기준 주문금액 비율 필터를 실거래 로직에 반영했다. 현재 기준은 `max_orderbook_ask_depth_ratio = 0.30`이며, 주문 예정 금액이 상위 5호가 매도잔량 금액의 30%를 넘으면 진입하지 않는다.
- `직전 스캔 대비 +1.0% 이상 상승` 종목 제외 필터를 추가했다. 기본값은 켜진 상태이며, 뒤늦은 당일 급등 추격을 줄이기 위한 안전장치다.
- 관련 테스트를 보강했다. `test_guards`, `test_predictor`, `test_runtime_safety`에 호가잔량 필터와 직전 스캔 급등 제외 로직 검증을 추가했다.
- 현재 설정값 기준으로 리플레이를 다시 확인했다. 표준 리플레이 총합은 `+10.6058%`, 직전 스캔 급등 제외까지 반영한 현재 로직 기준 리플레이 총합은 `+11.0196%`였다.
- 현재 리플레이 해석에서는 `selected`보다 `unselected`를 계속 우선 기준으로 본다. 최근 필터 구조와 자본 배분 방식이 많이 바뀌어 과거 `selected` 결과만으로 현재 전략을 설명하기 어렵기 때문이다.
- `2026-06-11`은 수익이 매우 좋았지만 특정 종목 쏠림이 컸다. 특히 `005090`이 9건 합계 `+6.4944%`를 기록해 해당 일자 총손익 `+10.6140%`의 큰 부분을 차지했다. 이 종목을 제외하면 그날 손익은 `+4.1196%`로 내려간다.
- 일자별 현재 로직 집계도 다시 확인했다. `2026-06-10`은 총매수금액 `4,210,915원`, `24건`, 익절 `13건`, 손실 `11건`; `2026-06-11`은 `9,923,685원`, `60건`, 익절 `46건`, 손실 `14건`; `2026-06-12`는 `8,187,250원`, `30건`, 익절 `20건`, 손실 `10건`; `2026-06-15`는 `8,225,310원`, `30건`, 익절 `22건`, 손실 `8건`이었다.
2026-06-15 11:37:37 +09:00
- `min_expected_return_percent`를 `0.25`에서 `0.30`으로 조정했고, 
- 전영업일 급등 필터는 기존 `15.0%` 대신 `100.0%`로 올려 사실상 비활성화했다. 
- 또한 종목 슬롯 수가 10개를 넘어야 하는 자본규모의 경우에는 슬롯 수를 무리하게 키우지 않고, 10개로 슬롯을 제한하고 투여자본금을 늘리는 형태로 수정했다.
- `trade_fills_audit.csv`에는 앞으로 실제 체결 기록만 남기고, `position_recovery`와 `sell_reconciliation` 같은 추정성 기록은 제외하도록 정리했다. 대신 DB `fills` 테이블에는 계속 보존한다.
- 장 마감 후 `daily_rev.csv`를 생성하도록 추가했다. 일자별로 시작 자본금, 총 수익금, 총 수수료, 총 세금, 총 매수금, 총 매도금, 매수금 기준 수익률, 시작 자본금 기준 수익률, 거래 종목 목록을 한 줄로 남긴다.
- GS피앤엘 사례를 점검한 뒤, 부분체결 뒤 추가 매수 체결이 늦게 확인되는 경우에도 그 추가 수량에 대해 후속 매도주문이 자동으로 들어가도록 수정했다.
- 세션 슬롯 계획의 기준값을 `계좌자산 추정치`가 아니라 `거래가능금액`으로 바꿨다. 이제 예열 시점에 슬롯 수와 슬롯당 자본금은 거래가능금액 기준으로 고정된다.
- 보유 중인 종목의 단기 흔들림을 나중에 분석할 수 있도록 `market_traces`에 `phase=active_position` 형태의 별도 추적 기록을 남기도록 추가했다.
- 손절매 기준은 내가 내일 하루 실험을 위해 수동으로 `1.0%`로 낮췄다. 이번 변경은 수익 극대화 목적이 아니라, 초단기 전략에서 매우 타이트한 손절선이 실제로 어떻게 작동하는지 확인하기 위한 하루짜리 실험 설정이다.
- `trend_filter`와 전영업일 급등 필터를 사실상 제거한 뒤, 후보군을 더 넓게 보는 방향으로 정리했다.
- `top_ratio`는 유지하되 전략 성과 해석 기준은 `selected`보다 `unselected` 리플레이 결과를 우선하도록 판단 기준을 바꿨다.
- `min_market_cap_krw`와 `min_trading_value_krw`를 완화하는 방향으로 검토했고, 현재 기준은 각각 `2500억`, `30억`으로 맞췄다.
- `max_spread_percent`는 `0.5`로 조정했다.
- 손절선은 여러 리플레이 비교 후 현재 기준을 `3.0%`로 맞췄다.
- 실거래와 백테스트의 매수 종료 시각을 함께 다룰 수 있도록 리플레이 엔진이 `start_buy_time`, `stop_buy_time`, `force_sell_time` 설정값을 따르도록 수정했다.
- 리플레이 엔진에서 장 마감 강제청산 관련 테스트를 보강했고, `11:30`, `13:00`, `14:00` 비교 결과를 확인했다.
- 최근 기준으로는 `14:00`까지 매수 가능 시간을 열어둔 쪽이 `unselected` 백테스트에서 가장 좋은 결과를 보여, 실거래 설정도 `14:00`으로 변경했다.
- `market_traces`에 `market_cap`, `trading_value`를 함께 기록하도록 저장 구조와 테스트를 수정했다.
- 백테스트 CSV와 실거래 로그가 섞이지 않도록 `Daily_bot/backtest/results` 폴더를 만들고, 기존 백테스트 결과물도 모두 그쪽으로 이동했다.
- `Daily_bot` 문서들을 현재 설정과 운영 기준에 맞게 전면 정리했다.
- 당시 실험 메모에는 `spread_expected_return_multiplier=1.5`가 적혀 있으나, 최신 실제 설정값은 `config/settings.yaml` 기준 `1.2`다.
- 동적 기대수익률 필터를 실거래와 리플레이에 함께 반영한 뒤, 현재 설정 재실행 기준 `unselected` 백테스트 결과는 총합 `-2.3006%`였다.
- 위 변경사항에 맞춰 README, 전략 문서, 인수인계 문서의 설정값과 설명을 다시 동기화했다.

2026-06-16 +09:00
- 최근 실거래 DB와 로그를 다시 보면서, 현재 손실의 핵심 원인을 `작게 자주 이기는 거래`가 아니라 `목표가가 바로 체결되지 않아 장시간 보유된 뒤 손절/강제청산되는 거래`로 판단했다. 5초~1분 안에 목표 지정가가 체결되는 거래는 여전히 전략의 수익 원천에 가깝지만, 오래 끌리는 포지션의 손실이 여러 번의 소익절을 지우는 구조가 확인됐다.
- 이에 따라 실거래 전략의 중심을 다시 `매수 체결 → 즉시 목표 지정가 매도 → 빠르게 체결되면 소익절` 구조로 돌리는 방향으로 정리했다. 목표가는 초기 의도와 동일하게 호가 예측가격에서 `sell_tick_offset = 1`틱을 뺀 가격을 기준으로 본다.
- 진입 필터도 초기 구조에 가깝게 단순화했다. 최종 필터는 `추세 필터 통과 여부`, `기대수익률이 최소 기준 이상인지`, `목표 매도가가 현재가보다 높은지` 세 가지를 핵심 조건으로 둔다. `max_prev_day_change_percent`, `max_spread_percent`, `spread_expected_return_multiplier`는 이번 판단에서는 실거래 최종 진입 조건의 중심축에서 제외했다.
- 포트폴리오형 슬롯 리필 방식은 폐기하고, `한 번에 최대 3개만 사고, 그 3개가 완전히 정리될 때까지 아무것도 안 하는 왕복거래형 봇`으로 되돌렸다. 설정은 `max_buy_count = 3`, `max_position_count = 3`, `max_slot_count = 3`을 기준으로 하고, 1~2개만 정리되어 빈 슬롯이 생겨도 새 종목을 보충하지 않도록 막았다.
- 이 변경의 이유는 손실 포지션을 보유한 상태에서 빈 슬롯만큼 계속 새 종목을 채우면, 이미 실패한 시그널의 리스크가 남아 있는 상태에서 새 리스크가 추가되기 때문이다. 왕복거래형은 손실 포지션 자체를 자동으로 해결하지는 못하지만, 손실이 여러 슬롯으로 번지는 것을 줄이고 한 번의 매수 묶음이 완전히 종료된 뒤에만 다음 매수 묶음을 시작하게 만든다.
- 손절매를 `0.5%`로 낮춰서 `익절이 평균 0.6% 수준이고 대부분 1분 안에 체결된다면, 굳이 길게 버티지 말고 손실날 종목은 빠르게 쳐내고 갈아타는 전략`을 백테스트 관점에서 검토했다. 그러나 수수료와 거래세를 반영하면 작은 익절 폭이 비용에 상당 부분 잠식되고, 0.5% 손절로 늘어나는 손절 빈도까지 고려했을 때 기대수익이 남지 않는 것으로 판단했다. 따라서 `0.5% 초타이트 손절 후 즉시 갈아타기` 아이디어는 폐기했다.
- 현재 우선순위는 손절선을 무작정 더 낮추는 것이 아니라, 애초에 수익이 났던 초단기 구조인 `즉시 목표 지정가 매도`와 `완전 정리 후 다음 3종목 묶음 진입`을 회복하는 것이다. 이후에도 손실이 반복된다면 다음 검토 대상은 손절률 자체보다 `목표가 미체결 후 시간 기반 탈출` 또는 `목표가가 바로 체결되지 않는 종목을 더 빨리 실패 처리하는 로직`이다.

2026-06-24 +09:00
- `Daily_bot` 현재 로직을 기준으로 최근 리플레이 구간(`2026-06-10 ~ 2026-06-17`)을 다시 확인했다. 비교 기준선은 `11:30 매수 마감`, `15:00 강제청산`, `top_ratio = 0.15`, `min_expected_return = 0.5`, 손절 OFF 조합으로 두었고, 총손익은 `-19,315원`이었다.
- `2026-06-17` 손실 원인을 역추적했다. 이날은 `012750 (-4.6572%)`, `001120 (-1.9827%)` 두 포지션이 15시 강제매도로 남으면서 하루 손익이 `-24,242원`까지 밀렸다. 반대로 이긴 거래들은 대부분 `+0.5% ~ +1.5%` 수준이라, 승률보다 손익비가 더 중요하다는 점을 다시 확인했다.
- 문제 포지션의 진입 시점(`012750 @ 10:02:04`, `001120 @ 10:08:14`)을 재구성했을 때, 당시 필터를 통과한 후보가 실제로는 각각 1개뿐이었다. 즉 “상위 후보 중 밀려 들어간 것”이 아니라 그 시각에 통과 후보가 거의 비어 있었다는 쪽이 더 정확했다.
- `-4.0%`, `-5.0%` 손절 실험을 먼저 돌렸다. `-4.0%`는 총손익이 `-19,359원`, `-5.0%`는 `-18,122원`으로 기준선과 큰 차이는 없었지만, `2026-06-17` 일손실은 줄어드는 경향이 있었다.
- `max_prev_day_change_percent`를 껐을 때는 총손익이 `-13,346원`으로 개선됐지만, 날짜별 편차가 커졌다. 특히 `6/15`는 `-9,830원`으로 악화되어 “필터 하나를 풀었다고 구조가 곧바로 좋아지지는 않는다”는 점을 확인했다.
- `top_ratio = 0.3`, `min_expected_return = 0.3`, `max_spread = 0.7` 조합은 총손익 `-60,035원`으로 크게 악화됐다. 후보군을 넓히되 기대수익률 문턱까지 낮추면, 저품질 후보가 너무 많이 섞여 전략 자체가 무너진다는 결론을 얻었다.
- `top_ratio = 0.5`, `min_expected_return = 0.5` 조합은 총손익 `-13,346원`으로 다시 개선됐다. 이때 `max_spread = 0.6`으로 더 조여도 결과는 사실상 동일했고, `spread_expected_return_multiplier = 2.0`은 오히려 총손익을 `-33,237원`까지 악화시켰다.
- `min_expected_return = 0.15`까지 낮추는 실험도 했다. 손절 없음은 `-53,561원`, `-4.5% 손절`을 더해도 `-60,847원`으로 더 나빠졌다. 이 구간에서는 기대수익률 문턱을 너무 낮추는 것이 가장 직접적인 악화 요인이었다.
- `max_spread_percent`를 아예 껐을 때와 `0.7`로 둘 때의 결과를 비교했다. 리플레이 결과가 완전히 동일했고 거래 파일도 같았다. 적어도 이번 로그 구간에서는 `0.7%`를 넘는 스프레드 종목이 실제 매수 대상으로 들어오지 않았다는 뜻으로 해석했다.
- `-4.5%` 손절과 손절 OFF를 직접 비교했다. `-4.5% 손절`로 잘린 5건 중 4건(80%)이 손절을 끄면 그대로 `15:00 강제매도/마감 강제청산`으로 이어졌다. 구체적으로 `000430`, `217590`, `012750`, `000240`가 그 사례였다. 즉 `-4.5% 손절이 강제매각과 매우 일치하여, 손절의 효과가 분명히 있음을 나타낸다`는 해석이 가능했다.
- 실코드와 백테스트 엔진 모두를 수정해서 `빈 슬롯이 생길 때마다 재매수`하지 않고, `3종목을 한 세트로 보고 세트가 전부 종료된 뒤에만 다음 세트를 매수`하는 구조로 바꿨다. 해당 변경에 맞는 단위 테스트도 추가했다.
- 이 `3종목 세트 재매수` 구조에서 `top_ratio = 0.5`, `min_expected_return = 0.5`, `-4.5% 손절` 조합을 다시 돌리면 총손익이 `-14,390원`이었다. 거래 수는 `68건 -> 15건`으로 크게 줄었고, 수수료/세금도 줄었지만 총손익은 슬롯 재매수 버전(`-13,207원`)보다 약간 나빴다.
- 세트 재매수 구조 안에서 `min_expected_return`과 `top_ratio`를 다시 튜닝했다. `min_expected_return = 0.4` 계열은 `top_ratio`가 `0.3 / 0.5 / 0.7` 어느 쪽이든 전부 `-23,939원`으로 동일하게 나빴다. `min_expected_return = 0.5` 계열은 `top_ratio`가 달라도 전부 `-14,390원`으로 묶였다. 이 구간에서는 세트 구조 아래에서 `top_ratio`보다 `min_expected_return` 쪽이 더 결정적이었다.
- `top_ratio`를 사실상 해제(`1.0`)한 뒤 `min_expected_return = 0.6`, `0.7`을 비교했다. `0.6`은 총손익 `-460원`으로 거의 손익분기점까지 개선됐고, `0.7`은 `-8,425원`으로 다시 나빠졌다. 이번 구간에선 `0.6`이 가장 균형이 좋았다.
- 마지막으로 `top_ratio = 1.0`, `min_expected_return = 0.6`, 세트 재매수 구조에서 손절만 껐다. 손절 ON(`-460원`)과 손절 OFF(`-860원`)를 비교하면 차이는 작지만, 손절 ON 쪽이 더 나았다. `2026-06-17` 일손실도 손절 ON이 `-5,799원`, OFF가 `-6,199원`이었다.
- 위 실험 결과를 반영해 `config/settings.yaml`은 `trend_filter = false`, `top_ratio = 1.0`, `min_expected_return = 0.6`, `max_spread = 0.0`, `stop_loss_percent = 4.5`로 맞췄다.

오늘 실험들을 종합하면, 현재 리플레이 구간에서 가장 일관된 방향은 `추세필터 OFF`, `top_ratio OFF(=1.0)`, `min_expected_return = 0.6`, `max_spread OFF`, `-4.5% 손절`, `3종목 세트 재매수` 조합이다. 특히 `-4.5% 손절`은 손절된 5건 중 4건이 손절 OFF 시 강제매각으로 이어질 만큼 강제청산 후보와 매우 높은 겹침을 보였고, 이는 손절의 효과가 분명히 있음을 나타낸다. 총합은 아직 완전한 플러스는 아니지만(`-460원`), 오늘 테스트한 조합들 중에서는 가장 손익분기점에 가까운 상태로 확인됐다.
2026-06-25 +09:00
- 데일리 봇 현재 상태를 다시 검토했다. 운영 프로세스는 살아 있었고, 후보 스캔은 정상적으로 돌았지만 최종 필터를 통과하는 종목이 거의 없어 장중 `selected=1` 신호가 0~1개 수준으로 줄어드는 상태였다.
- EOD 정산 경로에서 `sell_reconciliation` 추정 매도 체결이 실제 브로커 체결로 대체된 뒤에도 남아 있을 수 있는 문제를 확인했고, `reconcile_broker_fills()`가 `purge_superseded_sell_reconciliation_fills()`까지 호출하도록 수정했다.
- 실운영 연결 상태도 다시 점검했다. Kiwoom 인증과 계좌조회는 정상 응답했고, 이전에 한 번 발생했던 `ConnectionResetError(10054)`는 재현되지 않았다.
- 실운영 설정을 조정했다. 먼저 `min_expected_return_percent`를 `0.6 -> 0.3`으로 낮춰 후보가 너무 마르는 문제를 완화했다.
- 그 상태에서 장중 최신 스캔을 확인한 결과, 스캔 후보는 `92개`였고 최종 통과 후보는 `1개`, 오늘 `selected=1` 신호도 `1개`까지 살아났다.
- 이후 전일 등락률 하한을 `-1.5 -> -1.0`으로 다시 조정했다. 너무 깊게 눌린 종목만 보지 않고 약세 범위를 조금 덜 극단적으로 좁혀보려는 의도다.
- 설정 변경 후 실운영 봇을 두 번 모두 재시작해서 현재 프로세스가 새 설정으로 올라오도록 맞췄다. 현재 실운영 기준 시간 설정은 `09:10 ~ 11:30` 신규 진입, `15:00` 강제 청산, `15:15` EOD 대조다.
- 백테스트도 같이 점검했다. `bot.sqlite3`로 바로 돌린 리플레이는 `2026-06-25` 하루치만 포함하고 있어 거래 `5건`, 승률 `80%`, 총합 `+1.7439%`가 나왔다.
- 멀티데이 검증을 위해 로그 CSV에서 리플레이 DB를 다시 만들었는데, 이때 과거 로그에는 `prev_day_change_percent` 컬럼이 없는 날짜가 많다는 점을 확인했다.
- 그래서 현재 라이브 설정을 그대로 과거 로그에 대입하면 `min_prev_day_change_percent` 필터 때문에 `2026-06-10 ~ 2026-06-17` 구간이 전부 탈락하고, 사실상 `2026-06-25`만 거래가 나오는 구조였다. 즉 멀티데이 리플레이가 비어 보이는 이유는 DB 부족이 아니라 과거 로그 포맷 부족과 현재 필터의 충돌이다.
- 비교용으로 `min_prev_day_change_percent` 하한만 끈 멀티데이 리플레이도 따로 돌렸다. 이 경우 거래 `19건`, 승률 `73.68%`였지만 총합은 `-4.6398%`로 음수였다.
- 오늘 작업 결과를 반영해 `README.md`, `docs/strategy_design.md`, `docs/CODEX_HANDOFF.md`를 현재 운영값 기준으로 다시 정리했다. 앞으로는 문서보다 `config/settings.yaml`을 우선 진실원천으로 보고, 오래된 리플레이 로그 해석 시 `prev_day_change_percent` 누락 여부를 먼저 확인해야 한다.
- 같은 날 추가 조정으로 `trend_filter = false`, `min_prev_day_change_percent = -1.0`, `min_expected_return_percent = 0.3` 조합을 유지한 채 실운영을 다시 올렸다. 의도는 "자주 사는 봇"이 아니라, 빡빡하게 잡은 필터를 드물게 통과하는 시점만 거래 대상으로 삼는 것이다.
- 라이브 루프의 `should_wait_for_full_batch_exit()` 대기 게이트를 꺼서, 한 종목이라도 보유 중이면 신규 진입을 전부 막던 구조를 제거했다. 이제는 포지션이 일부 열려 있어도 남은 슬롯이 있으면 계속 스캔하고, 조건을 통과한 새 종목이 나오면 3슬롯이 찰 때까지 추가 진입할 수 있다.
- 이 변경의 핵심 의도는 "필터를 느슨하게 해서 매수를 늘리기"가 아니라, 희소한 신호가 장중 서로 다른 시점에 발생할 때 기존 보유 종목의 매도 체결을 기다리느라 다음 기회를 구조적으로 버리지 않게 만드는 것이다.
- 슬롯 운용 철학은 `3슬롯을 먼저 다 채우고, 각 슬롯은 동일한 기준과 동일한 비중으로 취급한다` 쪽으로 정리했다. 시점에 따라 성공률이 달라진다고 보지 않으므로 첫 종목에 더 싣고 후속 종목에 덜 싣는 방식은 채택하지 않았다.
- 재기동 직후 최신 스캔 사이클에서는 `selected=1` 후보가 `0개`였다. 즉 추가 매수가 바로 붙지 않은 이유는 슬롯 부족이 아니라, 현재 필터를 실제로 통과한 종목이 없었기 때문이다. 이 결과는 현재 의도와도 맞는다. 평소에는 거의 비어 있고, 가끔만 통과하는 신호를 잡는 구조로 보는 편이 정확하다.
2026-06-26 +09:00
- Today only replay was unusually volatile, so I did not want to lock the live bot to `min_expected_return = 0.9` just because the day rewarded the stricter filter shape.
- I set the live config and replay default to `min_expected_return_percent = 0.8` as the more conservative compromise between frequency and quality.
- The intent of `0.8` is to keep weak candidates out more aggressively than `0.7`, while avoiding the "signal drought" risk that can happen when `0.9` is used on calmer sessions.
- Operational note: if live scans become too sparse and selected signals dry up too often, lower `min_expected_return_percent` from `0.8` back to `0.7`.
2026-06-27 +09:00

## TODO: 호가 기반 예상가 모델 검증

### 핵심 가설

데일리 봇의 현재 예상가 계산 로직은 다음 가정에서 출발한다.

> 가격은 수요와 공급에 의해 결정된다.  
> 호가창은 각 가격대별 수요와 공급을 실시간으로 보여주는 가장 상세한 데이터다.  
> 따라서 현재 호가창의 수요·공급 불균형을 분석하면, 그 불균형이 해소되었을 때 도달할 단기 균형가격을 역산할 수 있다.

즉, 현재의 `expect_price`는 단순한 기술적 지표가 아니라 **현재 시점의 호가 불균형이 안정되었을 때 도달할 가능성이 있는 단기 균형가격**으로 본다.

### 현재 한계

현재 모델은 매수 판단 시점의 **단일 호가 스냅샷**만 사용한다.

따라서 이후에 새로 들어오는 주문, 취소되는 주문, 갑작스러운 시장가 주문, 참여자 심리 변화는 계산할 수 없다.  
이 정보들은 매수 시점에는 원천적으로 알 수 없는 미래 정보다.

그러므로 일부 손실 거래는 현재 모델의 오류라기보다, 미래 주문 흐름을 알 수 없기 때문에 발생하는 불가피한 결과로 본다.

### 향후 검증 항목

충분한 실거래 데이터가 쌓인 뒤, 기존 DB와 로그만 이용해서 아래 항목을 검증한다.

- 예상수익률과 실제 실현수익률의 상관관계
- 예상가와 실제 도달 최고가의 일치도
- 예상수익률 구간별 목표가 도달 확률
- 예상수익률 구간별 평균 실현수익률
- 예상가 또는 목표가까지 도달하는 데 걸린 평균 시간
- 호가 불균형이 클수록 도달 시간이 길어지는지 여부
- `0.6%`, `0.7%`, `0.8%`, `0.9%` 기준별 손익비와 거래 수 차이

### 운영 원칙

당장은 기록 구조를 추가로 복잡하게 만들지 않는다.

현재처럼 `market_traces`, `signals`, `orders`, `fills`, `account_traces`를 충실히 남기고, 분석은 이후 별도 백테스트/리포트 스크립트에서 수행한다.

현재 단계의 목표는 로직을 더 복잡하게 만드는 것이 아니라, **호가 기반 예상가 모델이 실제 예측력을 가지는지 충분한 표본으로 검증하는 것**이다.

2026-07-01 +09:00
- 최근 누적 로그 구간(`2026-06-26`, `2026-06-29`, `2026-06-30`, `2026-07-01`)을 기준으로 `min_expected_return_percent` 민감도 실험을 다시 돌렸다. 비교한 기준은 `0.8`, `0.75`, `0.73`, `0.71`, `0.7`이다.
- `0.8` 리플레이 결과는 `12건`, `10승 2패`, 거래 수익률 합계 `+8.6621%`였다. 후보 총합은 `152개`였고, `2026-07-01`에는 후보가 `1개`만 남았다.
- `0.75` 리플레이 결과는 `12건`, `11승 1패`, 거래 수익률 합계 `+11.7408%`였다. 후보 총합은 여전히 `오늘 1개` 수준으로 매우 적었다.
- `0.73` 리플레이 결과는 `13건`, `12승 1패`, 거래 수익률 합계 `+12.3069%`였다. 일별 후보 수는 `160 / 17 / 33 / 1`로 늘어났지만, `2026-07-01`은 여전히 `005090` 한 종목만 통과했다.
- `0.71` 리플레이 결과는 `15건`, `14승 1패`, 거래 수익률 합계 `+13.5171%`였다. 일별 후보 수는 `219 / 18 / 49 / 6`, 전체 후보 총합은 `292개`였다. `2026-07-01` 하루만 따로 보면 거래 `3건`, `3승 0패`, 일손익 `+4,918원`이었고 후보 총합도 `6개`로 확인됐다.
- `0.7` 하루 재검증에서도 `2026-07-01` 단일 일자 결과는 거래 `3건`, `3승 0패`, 일손익 `+4,918원`이었지만, 기준을 더 낮출수록 후보 반복 노출이 늘고 느슨한 구간에서 불필요한 후보가 다시 섞일 여지가 커진다고 판단했다.
- 이번 비교에서 핵심은 `0.73 ~ 0.75`가 총손익과 후보 품질 면에서는 나쁘지 않았지만, `2026-07-01`처럼 실제 운영 중 후보가 마르는 날에는 너무 빡빡하게 작동한다는 점이었다. 반대로 `0.7`은 오늘 성과는 좋았지만 문턱을 굳이 다시 낮춰 약한 후보를 더 받을 이유가 충분하지 않았다.
- 그래서 최종 타협값을 `0.71`로 정했다. 이유는 세 가지다. 첫째, 최근 누적 로그 기준 총손익이 가장 좋았고(`+13.5171%`) 손실 거래도 `2026-06-30 006110` 한 건만 남았다. 둘째, `2026-07-01` 같은 날에도 후보가 `6개`로 유지돼 신호 고갈이 덜하다. 셋째, `0.7`보다 아주 약간 더 보수적이라 품질을 크게 해치지 않으면서도 `0.73+`처럼 후보를 과도하게 말리지 않는다.
- 운영 변경으로 실거래 설정과 백테스트 기본값을 둘 다 `0.71`로 맞췄다. `config/settings.yaml`의 `strategy.min_expected_return_percent`와 `backtest/replay_market_traces.py`의 `--min-expected-return` 기본값을 모두 `0.71`로 변경했다.
- 이번 변경의 의도는 "오늘 하루 성과가 좋아 보인 숫자"를 그대로 채택하는 것이 아니라, 최근 누적 로그에서 `후보 수 부족`과 `후보 품질 저하` 사이 균형이 가장 안정적인 값을 기본 기준으로 고정하는 데 있다.
2026-07-04 +09:00
- `2026-06-26 ~ 2026-07-03` 누적 로그 전체를 기준으로 `min_expected_return_percent` 추가 민감도 테스트를 다시 돌렸다. 비교 구간은 `0.4`, `0.45`, `0.5`, `0.55`, `0.6`, `0.65`다.
- 전체 기간 합산 결과는 `0.6 = +18.3335% (26 trades, 21W 5L)`, `0.65 = +16.6261% (23 trades, 18W 5L)`, `0.4 = +15.8675% (22 trades, 16W 6L)`, `0.5 = +13.6714% (24 trades, 19W 5L)`, `0.45 = +11.0351% (18 trades, 12W 6L)`, `0.55 = +10.0844% (24 trades, 19W 5L)` 순으로 나왔다.
- 이번 구간에서는 `min_expected_return`을 낮춘다고 거래 수가 단조롭게 늘고 성과가 부드럽게 변하는 구조가 아니었다. 문턱값에 따라 잡히는 종목 구성이 달라져 `0.5`, `0.55`처럼 중간값이 오히려 더 나빠지는 구간이 확인됐다.
- 전체 성과만 보면 기본 운용 기준으로는 여전히 `0.6`이 가장 균형이 좋다. 수익 합계가 가장 높고 승률도 유지됐으며, 거래 수도 과도하게 마르지 않았다.
- 다만 이번 질문의 핵심은 "고변동 100 장이 아니라, 변동성이 훨씬 작은 장에서도 후보가 마르지 않게 할 하한선이 어디냐"는 점이라서, 로그 안에서 상대적으로 가장 조용했던 날(`2026-06-30`)의 후보 밀도를 threshold별로 따로 봤다.
- `2026-06-30` 기준 `expect_revenue_percent` 이상 티커 수는 `0.7 -> 8`, `0.65 -> 9`, `0.6 -> 10`, `0.55 -> 12`, `0.5 -> 14`, `0.45 -> 19`, `0.4 -> 23`이었다. 즉 저변동 쪽으로 갈수록 `0.7`, `0.6`은 후보가 빠르게 마르고, `0.4`부터 후보 풀이 눈에 띄게 넓어진다.
- 그래서 현재 증거 기준 결론은 두 층으로 나뉜다. 기본값은 계속 `0.6`이 가장 합리적이지만, 나중에 변동성이 크게 줄어 후보가 마르는 구간에 대응할 저변동 fallback 값으로는 `0.4`가 가장 유력하다.
- 아직 실제 "변동성 10"에 해당하는 별도 로그를 갖고 있지는 않으므로, `0.4`는 확정값이라기보다 현 고변동 로그 안에서 상대적으로 조용한 날들을 근거로 잡은 추정 하한선이다. 현재 데이터만 놓고는 `0.4` 아래로 더 내릴 근거는 없고, 저변동 대응용 자동 전환 규칙을 만든다면 우선 `0.6 -> 0.4` 2단계 구조로 시작하는 편이 타당해 보인다.
2026-07-13 +09:00
- 실거래 로그 기준으로 손절 후 같은 종목을 같은 날 다시 진입하는 사례가 있었고, 그 재진입도 추가 손실로 끝난 경우를 확인했다.
- 그래서 실거래 코드에 `손절 종목 당일 재진입 금지`를 추가했다. 손절이 실제 체결된 티커는 그날 남은 스캔 동안 신규 진입 후보에서 제외한다.
- 의도는 `재매수 허용`의 장점은 유지하되, 이미 당일 흐름이 깨졌다고 확인된 동일 종목만 국소적으로 차단해서 하방 리스크를 줄이는 것이다.
2026-07-13 +09:00
- 메인 루프에서 `poll_and_record_new_fills()` 이후에도 이전에 읽어둔 `positions/open_orders/active_tickers`를 그대로 써서, 체결 반영이 한 루프 늦게 신규 진입과 재진입 판단에 반영되는 지연 가능성을 확인했다.
- 그래서 실코드에 `post-fill account refresh`와 `post-scan account refresh`를 추가해, 같은 루프 안에서 최신 계좌 상태로 활성 티커 수와 빈 슬롯, 후보 제외 여부를 다시 계산하도록 수정했다.
- 이 변경은 유니버스 전체를 매 스캔마다 다시 도는 구조 자체를 바꾸는 것이 아니라, 체결 반영이 불필요하게 한 템포 늦던 운영상 지연만 줄이는 목적이다.
2026-07-13 +09:00
- 최근 실거래와 백테스트 비교에서, 현재가에서 먼 호가잔량이 기대수익률을 과대평가할 수 있다는 가설을 확인했고, 이를 줄이기 위해 매수/매도 양쪽 모두에 동일한 선형 감쇠를 넣는 방향으로 정리했다.
- 여러 감쇠 강도 중 현재 기본 운영값은 가장 강한 구간인 `1.0 -> 0.1` 대칭 감쇠로 맞췄다. 이 값은 후보 후처리 필터가 아니라 `expect_price` 계산식 자체에 들어간다.
- 동시에 최근 관찰 구간에서는 장중 손절이 장 후반 반등 가능성을 너무 일찍 잘라낸다고 판단해, 실코드와 백테스트 기본값 모두 `stop_loss_percent = 0.0`, `stop_loss_tick_count = 0`, `stop_loss_tick_multiplier = 0.0`으로 맞췄다.
- 이번 변경의 의도는 약한 후보를 더 많이 받으려는 것이 아니라, 먼 호가잔량의 허수성 영향을 줄여 기대수익 계산을 더 보수적으로 만들고, 손절이 장 후반 반등 가능성을 지나치게 훼손하는 문제를 완화하는 것이다.
- 문서도 같은 기준으로 전면 재정리했다. 특히 `curr_strategy.txt`와 `docs/DAILY_BOT_LOGIC_REFERENCE.md`는 “이 프로젝트를 전혀 모르는 사람이 읽어도 구조를 파악하고, 같은 전략을 다시 만들 수 있는가”를 기준으로 다시 작성했다.
2026-07-15 +09:00
- 후보 풀이 다시 너무 넓어지는 것을 막기 위해 `top_ratio`를 다시 활성화했다.
- 현재 운영값은 `top_ratio = 0.25`이며, 기대수익률 계산과 0.7 기준 필터를 통과한 후보 중 상위 25%만 실제 조합 선택 단계로 넘긴다.
- 변경 의도는 기대수익률 자체는 유지하되, 같은 배치 안에서 상대적으로 약한 후보까지 섞여 들어오는 것을 줄이는 것이다.
- 이 변경에 맞춰 `settings.yaml`, README, 전략 문서, 인수인계 문서, 재구성 문서를 모두 현재 구조 기준으로 다시 맞췄다.
2026-07-16 +09:00
- 전일 종가 기준 과열 필터 상한을 `1.0%`에서 `10.0%`로 완화해 후보 수를 늘렸다.
- 동시에 호가 거리별 선형 감쇠의 마지막 가중치를 `0.1`에서 `0.0`으로 더 낮춰, 현재가에서 먼 호가 잔량은 기대가격 계산에 거의 기여하지 않도록 더 강하게 눌렀다.
- 의도는 전일 상승률 필터로 후보를 과하게 막지 않되, 실제 진입 품질은 호가 모델 쪽에서 더 보수적으로 보정하는 것이다.
